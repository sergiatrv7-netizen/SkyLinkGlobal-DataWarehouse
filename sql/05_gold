# Gold Layer

This folder contains the SQL scripts used to create the Gold layer.

The Gold layer implements the dimensional model with fact and dimension tables optimized for business intelligence and reporting.
