# Database Schema

This folder contains the SQL scripts responsible for creating the database structure.

These scripts initialize the required schemas used throughout the project, providing the foundation for the Bronze, Silver, and Gold layers of the Medallion Architecture.
