# Database Schema

This folder contains the SQL scripts responsible for initializing the database structure.

The scripts included here create the required database schemas, indexes, and constraints that serve as the foundation for the SkyLink Global Airline Data Warehouse.

These objects are executed before creating the Bronze, Silver, and Gold layers.
